services:
  Memer-TESTING:
    build: .
    container_name: MemerV3
    env_file: .env
    restart: unless-stopped
    volumes:
      - .:/app
      - ./data:/app/data
      - ./sounds:/app/sounds
      - ./logs:/app/logs
    ports:
      - "${WEB_PORT:-3001}:3000"